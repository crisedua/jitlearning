node_modules/
.next/
out/
build/

# Secrets — never commit these
.env
.env.local
.env*.local

.vercel/

*.tsbuildinfo
next-env.d.ts
.DS_Store

# Source material and derived study guides. Book content is copyrighted —
# keep it out of a public repo. Ingested via `npm run ingest -- ./knowledge`.
knowledge/
